{
  "name": "SQL Explorer Demo Installation",
  "description": "A barebones SQL Explorer setup, which can easily be deployed to Heroku.",
  "image": "heroku/python",
  "repository": "https://github.com/chrisclark/django-sql-explorer",
  "keywords": ["python", "django", "sql"],
  "env": {
    "DJANGO_SECRET_KEY": {
      "description": "The secret key for the Django application.",
      "generator": "secret"
    }
  },
  "environments": {
    "test": {
      "scripts": {
        "test": "./manage.py test --debug-mode"
      }
    }
  }
}
